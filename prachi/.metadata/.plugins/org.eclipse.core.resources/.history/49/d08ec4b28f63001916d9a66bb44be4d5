import java.util.Comparator;

public class Capna implements Comparator<Try2>
{

	@Override
	public int compare(Try2 o1, Try2 o2) {
		if(o1.getMarks()==o2.getMarks())
		return 0;
		else if (o1.getMarks() >o2.getMarks())
		return 1;
		else if(o1.getMarks() < o2.getMarks())
		return -1;
		else
		return 0;
				}

	 
	
}
